import TextInput from '@/app/_components/common/TextInput';
import React from 'react';

export default function CreateCategoryForm() {
  return (
    <form action="submit">
      <TextInput value={} handleChange={} placeholder={} />
    </form>
  );
}
